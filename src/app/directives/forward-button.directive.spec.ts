import { ForwardButtonDirective } from './forward-button.directive';

describe('ForwardButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ForwardButtonDirective();
    expect(directive).toBeTruthy();
  });
});
